# Claude Code instructions

Follow `AGENTS.md` for repository rules.

Important portfolio convention: strategically important public projects use the
B + C pattern:

- featured homepage treatment in `index.html`;
- dedicated case study page under `projects/`.

ContextVault is the current example: `index.html`, `projects/contextvault.html`
and `assets/contextvault-architecture.svg`.
